/**
 * Maximum number of guest rows (people receiving invites) per event.
 * This does NOT count their invitees / plus-ones (inviteCount),
 * only the distinct guest entries.
 */
export const MAX_GUESTS_PER_EVENT = 2000;
